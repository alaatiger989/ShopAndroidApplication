<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity android:name=".Store.ProductDetails.ProductDetailsActivity" android:noHistory="true"></activity>
        <activity android:name=".Store.ShowStorePackage.ShowStoreActivity"  android:noHistory="true"/>
        <activity android:name=".Store.UpdateSingleDetailForProduct.UpdateSingleDetailProductActivity"  android:noHistory="true"/>
        <activity android:name=".TestActivity"  android:noHistory="true"/>
        <activity android:name=".Store.AddNewProductPackage.StoreActivity"  android:noHistory="true"/>
        <activity android:name=".MainActivity"  android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>